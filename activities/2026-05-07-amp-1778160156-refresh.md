# 2026-05-07 — Amp `0.0.1778160156-gb2be4b` refresh

## Context

User updated Amp CLI and asked whether `read_thread` extraction behavior changed, specifically whether it still uses `GEMINI_2_5_FLASH_LITE_PREVIEW_09_2025` despite that model not appearing on `https://ampcode.com/models`.

## Findings

- Current `~/.amp/bin/amp --version`:
  - `0.0.1778160156-gb2be4b`
  - released `2026-05-07T13:26:32.089Z`
- `read_thread` implementation is unchanged from the prior inspected binary:
  - validates thread id
  - checks `threadService.get(threadID)`
  - otherwise fetches `/api/threads/<threadID>.md`
  - runs `kt0(...)` extraction
  - extraction schema remains `{ relevantContent: string }`
  - extraction prompt still wraps Markdown in `<mentionedThread>...</mentionedThread>` and asks for goal-relevant extraction
- The extraction model constant still resolves as:
  - `Gl0 = K9.GEMINI_2_5_FLASH_LITE_PREVIEW_09_2025.name`
  - model name: `gemini-2.5-flash-lite-preview-09-2025`
  - context window in binary: `1048576`
  - max output tokens in binary: `65535`
- The model is an internal/binary model constant and may not be listed on public `ampcode.com/models`; absence from the public model page does not mean Amp is not using it internally for tool extraction.
- `free` mode remains invalid in CLI mode choices.
- Tool lists remain broadly aligned with our previous extraction: smart/deep/rush/large all include `read_thread` and `find_thread`; MCP tools appear directly as `mcp__server__tool` names.

## Connector impact

No code change required from this Amp update. The connector's local `read_thread` extraction parity remains directionally aligned, though it uses connector-local provider inference instead of Amp's internal Vertex/Gemini extraction model.

## Notes

The `package.json` version inside `~/.amp/package` still reports an older npm package version; `~/.amp/bin/amp --version` is the reliable runtime version string.
